export const restPresets = {};
export const graphqlPresets = {};
export const compatPresets = {};

// Legacy default export expected by higher-level barrel (standalone/src/models/project/project/index.ts)
// Keep pointing to the compatibility presets collection.
export default compatPresets;
