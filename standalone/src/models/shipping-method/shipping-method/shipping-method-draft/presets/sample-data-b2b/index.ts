import premiumShippingMethod from './premium-shipping-method';
import standardShippingMethod from './standard-shipping-method';

const presets = {
  premiumShippingMethod,
  standardShippingMethod,
};

export default presets;
