import { RestModelBuilder, GraphqlModelBuilder } from './builders';
export * from './types';

export const CartValuePriceTierDraftRest = {
  random: RestModelBuilder,
};

export const CartValuePriceTierGraphql = {
  random: GraphqlModelBuilder,
};
