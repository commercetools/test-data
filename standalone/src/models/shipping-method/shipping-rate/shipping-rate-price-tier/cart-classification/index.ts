import { RestModelBuilder, GraphqlModelBuilder } from './builders';
export * from './types';

export const CartClassificationPriceTierDraft = {
  random: RestModelBuilder,
};

export const CartClassificationPriceTierGraphlql = {
  random: GraphqlModelBuilder,
};
