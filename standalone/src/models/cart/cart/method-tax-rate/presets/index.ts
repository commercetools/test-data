import * as examplePresets from './with-all-fields/with-all-fields';

export const restPresets = {
  example: examplePresets.restPreset,
};
export const graphqlPresets = {
  example: examplePresets.graphqlPreset,
};
