import * as withProductSelectionOnly from './with-product-selection-only/with-product-selection-only';

export const graphqlPresets = {
  withProductSelectionOnly: withProductSelectionOnly.graphqlPreset,
};
