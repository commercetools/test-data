import { graphqlPreset } from './with-all-fields/with-all-fields-preset';

export const graphqlPresets = {
  withAllFields: graphqlPreset,
};
export const compatPresets = {
  withAllFields: graphqlPreset,
};
