import { type TModelFieldsConfig } from '@/core';
import type { TRecurrencePolicyScheduleInputGraphql } from './types';

export const graphqlFieldsConfig: TModelFieldsConfig<TRecurrencePolicyScheduleInputGraphql> =
  {
    fields: {
      standard: null,
      dayOfMonth: null,
    },
  };
