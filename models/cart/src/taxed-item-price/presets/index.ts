import * as withAllFields from './with-all-fields/with-all-fields-presets';

export const restPresets = {
  withAllFields: withAllFields.restPreset,
};

export const graphqlPresets = {
  withAllFields: withAllFields.graphqlPreset,
};
