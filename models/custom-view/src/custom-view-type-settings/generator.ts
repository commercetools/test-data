import { Generator } from '@commercetools-test-data/core';
import { CustomViewTypeSettings } from './types';

const generator = Generator<CustomViewTypeSettings>({
  fields: {},
});

export default generator;
