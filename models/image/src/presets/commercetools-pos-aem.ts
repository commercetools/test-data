import Image from '../builder';

const commercetoolsPosAem = () =>
  Image()
    .url('https://bit.ly/3lCCAiE')
    .label('commercetools sits between POS and AEM')
    .dimensions({ w: 800, h: 450 });

export default commercetoolsPosAem;
